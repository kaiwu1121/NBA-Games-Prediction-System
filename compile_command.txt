  arma::mat dataset;
  data::Load("2014_season_summary.csv", dataset, true);
    // Find out what dimension we want.
   size_t newDimension = 29;
  // Get the options for running PCA.
  const size_t scale =true;
  // Perform PCA.
  PCA p(scale);
  double varRetained;
 //  varRetained = p.Apply(dataset, 1);
 varRetained = p.Apply(dataset, newDimension);
std::cout<<varRetained<<endl;
  // Now save the results.
  data::Save("pca_output.csv", dataset);	
  arma::mat dataset_pca;
  data::Load("pca_output.csv", dataset_pca, true);
